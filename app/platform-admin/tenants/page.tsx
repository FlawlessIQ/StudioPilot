import { Building2 } from "lucide-react";
import { AdminShell } from "@/components/platform/admin-shell";
import { StatusBadge } from "@/components/ui/status-badge";
export default function TenantsPage(){return <AdminShell active="Tenants"><header><div><p className="eyebrow">Tenant control</p><h1>Tenants</h1><p>Account status, subscription, and risk without entering tenant data.</p></div></header><section className="panel ops-table"><div className="ops-row ops-head"><span>Tenant</span><span>Plan</span><span>Users</span><span>Health</span></div>{[["Alder & Muse Photography","Studio","3 / 5","Healthy"],["Northlight Creative","Multi-Brand","11 / 15","Healthy"],["Fieldhouse Sports Media","Studio","5 / 5","Attention"]].map(row=><div className="ops-row" key={row[0]}><span><Building2 size={17}/><strong>{row[0]}</strong></span><span>{row[1]}</span><span>{row[2]}</span><StatusBadge tone={row[3]==="Healthy"?"success":"warning"}>{row[3]}</StatusBadge></div>)}</section></AdminShell>}
