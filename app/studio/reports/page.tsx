import { BarChart3,CalendarRange,ShieldCheck } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
import { PrintReportButton,ReportCsvExport } from "@/components/post-event/post-event-actions";
import { reportBars,reportMetrics } from "@/config/post-event-demo-data";

export default function ReportsPage(){return <AppShell active="Reports"><div className="post-event-page report-page"><header className="page-heading"><div><p className="eyebrow">Studio intelligence</p><h1>Reports</h1><p>Trace conversion, revenue references, readiness, cycle time, and operational reliability.</p></div><div className="report-actions"><ReportCsvExport/><PrintReportButton/></div></header><form className="report-filters" method="get"><label>From<input type="date" name="from" defaultValue="2026-01-01"/></label><label>To<input type="date" name="to" defaultValue="2026-07-26"/></label><label>Project type<select name="type" defaultValue="all"><option value="all">All project types</option><option value="wedding">Wedding</option><option value="corporate">Corporate</option><option value="sports">Sports</option></select></label><button className="button button-dark" type="submit"><CalendarRange size={16}/> Apply filters</button></form><section className="report-metrics">{reportMetrics.map(item=><article className="panel" key={item.label}><small>{item.label}</small><strong>{item.value}</strong><span>{item.detail}</span></article>)}</section><div className="report-layout"><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Lead sources</p><h2>Booked inquiries by source</h2></div><BarChart3/></div><div className="report-bars">{reportBars.map(item=><article key={item.label}><span><strong>{item.label}</strong><small>{item.count} bookings</small></span><i><b style={{width:`${item.value}%`}}/></i></article>)}</div></section><aside className="panel report-source-note"><ShieldCheck/><h2>Source labels stay explicit.</h2><p>Financial cards use QuickBooks-synced references and display their last synchronization time. Readiness and automation values come from deterministic internal records.</p><small>QuickBooks sync · Jul 26, 6:12 PM</small></aside></div></div></AppShell>}
