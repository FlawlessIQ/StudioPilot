{
  "name": "studiohub-functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.4.0",
    "mammoth": "^1.10.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/busboy": "^1.5.4",
    "typescript": "^5.9.0"
  }
}
