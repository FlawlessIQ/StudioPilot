# StudioCue production audit #2 — 2026-09-04 (post-fix regression + new findings)

Second full production walk, run after shipping 18 fixes from audit #1
(`docs/ux-walkthrough-production-2026-09-04.md` + `docs/fix-backlog-2026-09-04.md`).
Purpose: (1) regression-verify the shipped fixes in the live flow, (2) find new
issues, (3) simulate dates to reach the phases audit #1 couldn't (final invoice,
event day, delivery, closeout, review, trial expiry).

## Setup
- **Fresh studio.** Owner `conor+studio2@flawlessiq.com`, password `Audit2-XbgMiKvaKM!`
  (throwaway; rotate/delete when done). Signup done by Conor in his real Chrome
  (the automation pane's reCAPTCHA/App Check token is refused — audit #1 P2 note).
- **Couple** `conor+couple2@flawlessiq.com` (readable via the shared inbox).
- **Crew** `conor@ad-helm.com` (Outlook — now that SPF is live, whether the crew
  invite reaches the inbox instead of Junk is the live re-test of P18).
- **Integrations:** Zoom + QuickBooks connected (Conor OAuth); **Dropbox and
  Google Calendar deliberately left off** — the direct regression test that the
  booking confirmation (P15) and consultation confirmation (P14) still send.
- **Regression watch-list (must behave as fixed):** P1,P2,P4,P5,P6,P7 (auth
  first-run), P8,P10,P11 (billing), P12 (OAuth denial), P13 (reply personalized),
  P14,P17 (email triggers), P15 (booking confirmation), P18 (SPF/deliverability),
  P20,P21 (portal), P23 (cancelled job).

## Findings
(logged as they arise; R-prefixed = regression of a shipped fix, N-prefixed = new)

